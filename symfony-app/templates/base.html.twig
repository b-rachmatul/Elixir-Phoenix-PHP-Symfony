<!DOCTYPE html>
<html lang="pl">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Phoenix API Panel{% endblock %}</title>
        
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">

        <style>
            body { background-color: #f8f9fa; }
            .card { border: none; box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075); }
            .table thead { background-color: #f1f3f5; }
            .filter-section { background-color: #fff; padding: 20px; border-radius: 8px; margin-bottom: 25px; }
        </style>
        
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark py-3 mb-5 shadow">
            <div class="container">
                <a class="navbar-brand d-flex align-items-center" href="{{ path('user_index') }}">
                    <i class="bi bi-lightning-charge-fill text-warning me-2"></i>
                    <span class="fw-bold tracking-tight">PHOENIX ADMIN</span>
                </a>
                
                <div class="navbar-nav ms-auto">
                    <span class="nav-link disabled text-uppercase small fw-semibold" style="letter-spacing: 1px;">
                        Panel Zarządzania
                    </span>
                </div>
            </div>
        </nav>

        <main class="container">
            {# Komunikaty Flash (Sukces/Błąd) #}
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label == 'success' ? 'success' : 'danger' }} alert-dismissible fade show border-0 shadow-sm mb-4" role="alert">
                        <i class="fas {{ label == 'success' ? 'fa-check-circle' : 'fa-exclamation-circle' }} me-2"></i>
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            {% block body %}{% endblock %}
        </main>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>